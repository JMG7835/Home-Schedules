services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=homeschedules'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '15432:5432'

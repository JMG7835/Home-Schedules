apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pg-data-claim
  labels:
    app: postgres
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi